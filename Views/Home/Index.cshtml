@model IndexViewModel

    @{
    ViewBag.Title = "Blog Website";
    ViewBag.Description = "A Blog That Contains Everything";
    ViewBag.Keywords = "Blog Programming Travel Nature Photography";
    var base_path = Context.Request.PathBase;

    //PaginationViewModel pagination = new PaginationViewModel();
    //int pageNo = 0; 

    //if(ViewBag.Pagination != null)
    //{
    //    pagination = ViewBag.Pagination;
    //    pageNo = pagination.CurrentPage;
    //}
}

    @*<h2>Home Page!!</h2>
    <img src="~/content/static/1.jpg" width=500px/> *@

    <div class="main-img">
        <span class="title"> Amazing Blog Site </span>
    </div>
    t
    <div class="container">
    @foreach(var post in Model.Posts)
    {
        var image_path = $"{base_path}/Image/{post.Image}";
        <a class="post" asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">

            <img src="@image_path"/>

            <span class="title">
                @post.Title
            </span>
        </a>
    }



    @*<div>
        @if(Model.PageNumber > 1)
        {
            <a asp-action="Index" asp-controller="Home"
                asp-route-category="@Model.Category" 
                    asp-route-pageNumber="@(Model.PageNumber - 1)">prev</a> 
        }
        @if (Model.NextPage)
        {
            <a asp-action="Index" asp-controller="Home"
                asp-route-category="@Model.Category" 
                    asp-route-pageNumber="@(Model.PageNumber + 1)">next</a> 
        }
    </div>
    *@

    </div>